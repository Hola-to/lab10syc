#pragma once
#include "conference.h"
#include <iostream>
#include <Windows.h>
#include <fstream>
#include "conference.h"
using namespace std;
void find_str(conference* con[], const name, size_t count);